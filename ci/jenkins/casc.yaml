jenkins:
  systemMessage: "Ian Portfolio Jenkins - 本机构建 / 远程部署"
  numExecutors: 2
  mode: NORMAL

  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "${JENKINS_ADMIN_PASSWORD:-admin123}"

  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false

credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope: GLOBAL
              id: "gitee-credentials"
              username: "${GITEE_USER}"
              password: "${GITEE_TOKEN}"
              description: "Gitee 访问令牌"
          - string:
              scope: GLOBAL
              id: "tianditu-token"
              secret: "${VITE_TIANDITU_TOKEN}"
              description: "天地图令牌"
          # ssh-remote-deploy 通过 Jenkins UI 凭据管理录入
          # 不写进 casc.yaml 避免 SSH 私钥入仓库
